using UnityEngine;
using System.Collections;

public class EndGameScoreMediator : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Canvas canvas = gameObject.GetComponent<Canvas>();
        canvas.enabled = true;
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
